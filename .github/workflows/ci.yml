name: 🤖 SonarCloud CI

#--------------------------------
# Gatilho(Trigger): Define quando o workflow será executado
#--------------------------------
on:
  push:
    branches:
      - main # Gatilho para pushes na branch main
  pull_request:
    types: [opened, synchronize, reopened] # Gatilho para pull requests


jobs:
  sonarcloud:
    name: 📡 SonarCloud Scan
    runs-on: ubuntu-latest

    steps:
      # 1. Baixar o código-fonte do repositório
      - name: 📥 Checkout do Código
        uses: actions/checkout@v4
        with:
          # fetch-depth: 0 é OBRIGATÓRIO para análises do SonarCloud
          # Ele garante que todo o histórico do Git seja baixado para saber o que é "New Code"
          fetch-depth: 0

      # 2. Configurar o ambiente Python
      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'  # Versão do Python utilizada no projeto

      # 3. Instalar dependências necessárias (inclusive pytest e coverage, se necessário)
      - name: 📦 Instalar Dependências
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # Garante que as ferramentas de teste estão instaladas
          pip install pytest pytest-cov

      # 4. Roda os testes e gera o relatório de cobertura
      # O Comando --cov=, calcula a cobertura do código na pasta especificada
      # O comando --cov-report=xml gera o relatório em formato XML, necessário para o SonarCloud
      - name: 🧪 Rodar Testes com Cobertura
        run: |
          # --cov=src: Analisa a pasta src
          # --cov-report=xml: Gera o arquivo coverage.xml para o Sonar ler
          pytest --cov=src --cov-report=xml

      # 5 Corrigir caminhos no arquivo coverage.xml (se necessário)
      - name: 🚑 Corrigir Caminhos no XML
        run: |
          # Esse comando procura filename=" e muda para filename="src/
          sed -i 's|filename="|filename="src/|g' coverage.xml

          # Debug: Vamos conferir se apareceu o src/ antes do nome
          echo "🔎 Verificando correção no coverage.xml:"
          cat coverage.xml | head -n 5

      # 6. Ajustar caminhos absolutos no coverage.xml para garantir compatibilidade com SonarCloud
      - name: 🚑 Corrigir Caminhos Absolutos no XML
        run: |
          sed -i 's|filename=".*src/|filename="src/|g' coverage.xml

          # Debug: Verificar se os caminhos foram ajustados corretamente
          echo "🔎 Verificando correção no coverage.xml após ajuste de caminhos absolutos:"
          cat coverage.xml | head -n 10

      # 7. Ajustar caminhos de forma robusta no coverage.xml
      - name: 🚑 Correção Definitiva de Caminhos no XML
        run: |
          sed -i 's|filename=".*src/|filename="src/|g' coverage.xml
          sed -i 's|<source>.*src|<source>src|g' coverage.xml

          # Debug: Exibir o conteúdo completo do coverage.xml para validação
          echo "🔎 Conteúdo do coverage.xml após ajustes definitivos:"
          cat coverage.xml

      # 8. Ajustar o elemento <source> no coverage.xml
      - name: 🚑 Corrigir Elemento <source> no XML
        run: |
          sed -i 's|<source>src|<source>C:\\Users\\tinal\\Projetos\\python_zero_a_ai\\src|g' coverage.xml

          # Debug: Exibir o conteúdo completo do coverage.xml para validação
          echo "🔎 Conteúdo do coverage.xml após ajuste do <source>:"
          cat coverage.xml

      # 9. Consolidar ajustes no coverage.xml
      - name: 🚑 Ajuste Consolidado no XML
        run: |
          sed -i 's|filename=".*src/|filename="src/|g' coverage.xml
          sed -i 's|<source>.*|<source>src|g' coverage.xml

          # Debug: Exibir o conteúdo completo do coverage.xml para validação
          echo "🔎 Conteúdo do coverage.xml após ajustes consolidados:"
          cat coverage.xml

      # 10. Ajuste final para cobertura
      - name: 🚑 Ajuste Final para Cobertura
        run: |
          sed -i 's|<source>.*|<source>src|g' coverage.xml

          # Debug: Exibir o conteúdo completo do coverage.xml para validação
          echo "🔎 Conteúdo do coverage.xml após ajuste final:"
          cat coverage.xml

      # 11. Enviar análise para o SonarCloud (ação oficial)
      - name: 🚀 Analisar com SonarCloud
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
